export type Site = {
  website: string;
  author: string;
  desc: string;
  title: string;
  email: string;
};

export type SocialObjects = {
  name: string;
  href: string;
  linkTitle: string;
}[];
